{
  "name": "cv-thomas-bourcey",
  "version": "1.1.0",
  "description": "Générateur multi-langues de CV (HTML, PDF, Markdown)",
  "main": "build/build.js",
  "scripts": {
    "build": "node build/build.js",
    "validate": "node build/build.js --validate-only",
    "lint": "eslint .",
    "test": "node --test",
    "serve": "npx serve ."
  },
  "dependencies": {
    "playwright": "^1.62.1",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "globals": "^17.11.0"
  }
}
